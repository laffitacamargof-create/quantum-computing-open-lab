services:
  - type: web
    name: qcol-motor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn qcol_server:app --bind 0.0.0.0:$PORT --timeout 120
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
